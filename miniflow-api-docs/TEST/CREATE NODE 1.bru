meta {
  name: CREATE NODE 1
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/api/bff/nodes/
  body: json
  auth: inherit
}

body:json {
  {
    "workflow_id": "{{WORKFLOW_ID}}",
    "name": "bruno_node_1",
    "description": "First node: calculates 10 + 5 = 15",
    "script_id": "{{SCRIPT_ID}}",
    "max_retries": 3,
    "timeout_seconds": 300,
    "priority": 10
  }
}

settings {
  encodeUrl: true
}
