meta {
  name: CREATE NODE 3
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/api/bff/nodes/
  body: json
  auth: inherit
}

body:json {
  {
    "workflow_id": "{{WORKFLOW_ID}}",
    "name": "bruno_node_3",
    "description": "Third node: adds result from node_2 + 7",
    "script_id": "{{SCRIPT_ID}}",
    "max_retries": 3,
    "timeout_seconds": 300,
    "priority": 10
  }
}

settings {
  encodeUrl: true
}
