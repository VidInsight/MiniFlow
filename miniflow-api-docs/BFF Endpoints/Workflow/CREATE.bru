meta {
  name: CREATE
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/bff/workflows/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Email Processing Workflow",
    "description": "Workflow for processing incoming emails and extracting attachments",
    "priority": 10
  }
}

settings {
  encodeUrl: true
}
