meta {
  name: LIST by WORKFLOW
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/bff/execution-inputs/?workflow_id={{WORKFLOW_ID}}
  body: none
  auth: inherit
}

params:query {
  workflow_id: {{WORKFLOW_ID}}
}

settings {
  encodeUrl: true
}
