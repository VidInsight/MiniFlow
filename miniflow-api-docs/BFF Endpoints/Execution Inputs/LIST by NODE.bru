meta {
  name: LIST by NODE
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/api/bff/execution-inputs/?node_id={{NODE_ID}}
  body: none
  auth: inherit
}

params:query {
  node_id: {{NODE_ID}}
}

settings {
  encodeUrl: true
}
