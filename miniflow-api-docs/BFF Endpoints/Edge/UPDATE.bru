meta {
  name: UPDATE
  type: http
  seq: 3
}

put {
  url: {{BASE_URL}}/api/bff/edges/{{EDGE_ID}}
  body: json
  auth: inherit
}

body:json {
  {
    "condition_type": "FAILURE"
  }
}

settings {
  encodeUrl: true
}
