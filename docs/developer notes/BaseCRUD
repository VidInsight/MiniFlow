Base CRUD:
def _create(self, session: Session, **model_data) -> ModelType:
1. **model_data validasyon -> Validation Error
2. Filter invalid fields if there are any -> logger warning
3. Create the data with try except
    ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _get_by_id(self, session: Session, record_id: str) -> Optional[ModelType]:
1. Get record with id
    1.1 if there is return the model
    1.2 if there is not return None
    ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _update(self, session: Session, record_id: str, int, **model_data) -> ModelType:
1. **model_data validasyon -> Validation Error
2. Object check with given record_id -> call self._get_by_id -> if not raise DatabaseQueryError
3. If object have updated_at create payload with current time
4. Filter invalid fields if there are any -> logger warning
5. Set attribute
---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _delete(self, session: Session, record_id: str) -> ModelType:
1. Object check with given record_id -> call self._get_by_id -> if not raise DatabaseQueryError
2. Delete object
---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


 def _exists(self, session: Session, record_id: str) -> bool:
 1. Create statment -> select(func.count(self.model.id)).where(self.model.id == record_id)
 2. execute query
 ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _count(self, session: Session) -> int:
 1. Create statment -> select(func.count(self.model.id)).where(self.model.id == record_id)
 2. execute query
 ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _get_all(self, session: Session, skip: int = 0, limit: int = 100, order_by: str = None) -> List[ModelType]:
1. Set limit to ->  min(limit, 1000)  # Memory protection
2. Create statment ->  select(self.model)
3. Order by odreder_by if
4. Add ofset and skip statment to statment ->  stmt.offset(skip).limit(limit)
4. Eecute statment
 ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _filter(self, session: Session, filters: Dict[str, Any], skip: int = 0, limit: int = 100, order_by_field: str = None) -> List[ModelType]:
1. Set limit to ->  min(limit, 1000)  # Memory protection
2. Create statment ->  select(self.model)
3. Loop for validation and query building
4. Ordering logic
5. Apply pagination
6. Execute Query
 ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError


def _count_with_filter(self, session: Session, filters: Dict[str, Any]) -> int:
1. Create statment ->         stmt = select(func.count(self.model.id))
2. Loop for validation and query building
3. Execute Query
 ---
    3.1 Except SQLAlchemyError -> DatabaseQueryError
    3.2 Except Exception -> DatabaseQueryError